node_modules
dist
dist-ssr
.DS_Store
*.local
.env
.env.*
!.env.example
!.env.production
# android/ TAKİP EDİLİYOR: derin link (AndroidManifest) ve imzalama ayarları
# native dosyalarda duruyor; klasör git'te olmazsa her `cap add android`
# bunları siliyordu. Klasörün kendi .gitignore'u derleme çıktılarını,
# local.properties'i ve kopyalanan web dosyalarını zaten dışarıda tutuyor.
ios
.wrangler
assets-raw
